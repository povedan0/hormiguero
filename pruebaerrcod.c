#include <stdio.h>

int main(){
    int num, val;

    printf("Para determinar si es par o impar introduce entero positivo: ");
    scanf("%d", &num);

    val = 2 * (int)(n/2);

    if (val == num)
        printf("%d par\n", num)
    else
        printf("%d impar\n", num);

    return 0;
}